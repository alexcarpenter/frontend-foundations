{% macro pageHeader(params) %}
<header class="c-page-header">
  <h1 class="c-page-header__title">{{ params.title }}</h1>
  {% if params.description %}
    <p class="c-page-header__description">{{ params.description }}</p>
  {% endif %}
</header>
{% endmacro %}